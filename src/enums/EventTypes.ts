export enum EventTypes {
  START_SESSION = 'start_session_event',
  JOIN_SESSION = 'join_session_event',

  // movies
  SORT_MOVIES_EVENT = 'sort_movies_event',
  VOTED_MOVIE_EVENT = 'voted_movie_event',
  CHOSEN_MOVIE_EVENT = 'chosen_movie_event',
}
